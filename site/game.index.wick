import {createGame, getActiveGame} from "./scripts/game.ts";

let game = null;

import board from "./components/board.wick";

function on_mount(){  game = getActiveGame() || createGame([],4,4); console.log(game) }

export default <div>
    <board model={game}/>
</div>;